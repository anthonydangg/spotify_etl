services:
  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_PASSWORD: mysecretpassword
    volumes: 
     - ./postgres:/docker-entrypoint-initdb.d

  adminer:
    image: adminer
    restart: always
    ports:
     - 8080:8080

